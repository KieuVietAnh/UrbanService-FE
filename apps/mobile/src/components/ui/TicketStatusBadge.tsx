import React from 'react';
import { AppBadge, AppBadgeProps } from './AppBadge';

export function TicketStatusBadge(props: AppBadgeProps) {
  return <AppBadge {...props} />;
}

export default TicketStatusBadge;
