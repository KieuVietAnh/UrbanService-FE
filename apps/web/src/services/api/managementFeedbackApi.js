import { managementFeedbackApi as sharedManagementFeedbackApi } from '@urbanmind/shared-api';

// Re-export the shared API to centralize logic
export const managementFeedbackApi = sharedManagementFeedbackApi;
